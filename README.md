# Skills

[![skills.sh](https://skills.sh/b/Team-Discipline/skills)](https://skills.sh/Team-Discipline/skills)

AI Agent용 Skill 정의 저장소입니다. Skills는 특정 작업을 수행하는 재사용 가능한 지침(instruction) 모음으로, AI 어시스턴트가 더 효과적으로 작업할 수 있도록 돕습니다.

## Available Skills

### git-commit-split

#### 상세 설명

파일 변경사항을 **의존성(dependency)** 과 **의도(intent)** 기준으로 분석하여 Prep → Core → Fix → Polish 순서의 작고 집중된 커밋들로 분할합니다. 각 커밋은 독립적으로 revert/cherry-pick 가능해야 하며, 커밋 로그를 시간순으로 읽으면 개발자의 의사결정 흐름이 드러나야 한다는 철학을 따릅니다.

단순히 `git add -p`를 대행하는 수준이 아니라, 변경된 모든 파일을 분석하여 의존성 그래프를 그리고, 가장 안전하면서도 이야기 흐름이 있는 커밋 순서를 제안합니다. 사용자 확인 후에만 staging/commit을 진행합니다.

#### 언제 사용하면 좋은가

| 상황 | 설명 |
|------|------|
| **커밋 분할 요청** | "커밋을 분할해줘", "커밋을 나눠줘" |
| **변경사항 선별 커밋** | "이것만 커밋하고 싶어", "변경사항을 정리해줘" |
| **작업 트리 정리** | "작업 트리가 너무 지저분해", "히스토리를 정리하고 싶어" |
| **PR 리뷰 대비** | "PR 제출 전에 커밋을 깔끔하게 만들고 싶어" |
| **여러 의도가 섞인 PR** | "이 PR에 여러 기능이 섞였어, 커밋을 분리해줘" |
| **force-push 필요 상황** | "분할하려면 force-push가 필요한데 괜찮아?" |
| **Partial staging (hunk 분할)** | "한 파일 안에 버그픽스와 리팩터링이 섞여 있어" |

#### 어떻게 사용하는가

1. **자동 감지** — AI 에이전트가 관련 요청을 감지하면 자동으로 스킬이 활성화됩니다.
2. **직접 호출** — "커밋 분할해줘" 또는 "git commit split"이라고 말하면 됩니다.
3. **대화형 모드** — 기본적으로 내러티브 계획을 표시하고 그룹별로 확인을 받습니다.
4. **비대화형 모드** — "바로 진행해줘"라고 말하면 모든 그룹을 일괄 처리합니다.
5. **내러티브 모드** — "커밋 스토리를 들려줘"라고 말하면 각 커밋에 왜 이 순서인지 설명이 포함됩니다.

프로세스: Change Analysis → Build Narrative Plan → User Confirm → Validate Independence → Stage & Commit → Verify Recoverability

#### 기대 효과

- **복구 용이성 향상**: 각 커밋을 독립적으로 revert 가능 → 특정 변경만 롤백해도 나머지가 깨지지 않음
- **히스토리 가독성 개선**: "무엇을" 바꿨는지가 아니라 "왜 이 순서로" 바꿨는지가 드러남
- **코드 리뷰 효율화**: 리뷰어가 커밋 단위로 맥락을 이해할 수 있어 리뷰 시간 단축
- **cherry-pick 가능**: 특정 커밋만 골라서 핫픽스 배포나 다른 브랜치에 적용 가능
- **PR 품질 향상**: 하나의 의도 = 하나의 커밋 원칙으로 PR의 변경 범위가 명확해짐
- **회귀 최소화**: 의존성 분석을 통해 중간 상태가 깨지는 커밋을 사전에 방지
- **협업 충돌 감소**: 잘게 나누어진 커밋은 merge 시 충돌 범위가 작고 파악이 쉬움

---

### quality-check

#### 상세 설명

저장소 코드 품질을 **세 가지 모드**(부분 변경, 최근 커밋, 전체 감사)로 점검하고 한국어 또는 영어로 간결한 결과를 보고합니다. 코드를 절대 수정하지 않으며, 테스트도 작성하지 않고 방향만 제시합니다. 기존 린트/테스트/타입체크 도구가 있다면 우선 실행하고 그 결과를 리포트에 통합합니다.

찾은 문제는 **P1(런타임 크래시/데이터 손실/보안)**, **P2(주요 경로 회귀/API 계약 위반)**, **P3(유지보수성/중복)** 등급으로 구분하여 우선순위를 명확히 합니다.

#### 언제 사용하면 좋은가

| 상황 | 설명 | 권장 모드 |
|------|------|----------|
| **PR 제출 전 검토** | "코드 변경사항을 리뷰해줘", "PR 보내기 전에 확인해줘" | Partial |
| **작업 중간 점검** | "지금까지 한 거 품질 검사해줘" | Partial |
| **최근 작업 히스토리 점검** | "지난 5개 커밋 리뷰해줘", "최근 작업 검사해줘" | Recent Commits |
| **릴리즈 전 전체 감사** | "전체 저장소 품질 감사해줘", "릴리즈 전에 코드베이스 점검해줘" | Full Audit |
| **온보딩/코드베이스 파악** | "이 프로젝트 전체 상태를 알고 싶어" | Full Audit |
| **버그 원인 분석** | "최근 변경에서 문제가 있었던 것 같아" | Recent Commits |
| **지속적 품질 관리** | "주간 코드 품질 리포트를 만들어줘" | Full Audit |

#### 어떻게 사용하는가

1. **자동 감지** — AI 에이전트가 품질 검사 요청을 감지하면 자동 활성화됩니다.
2. **Partial 모드** — `git status --short`로 변경 파일 식별 → 해당 파일 + 직접 영향받는 계약에 한정 검사
3. **Recent Commits 모드** — `git diff HEAD~n..HEAD`로 n개 커밋 범위 검사 → 기존 도구 실행 → 커밋별/파일별 리포트
4. **Full Audit 모드** — 저장소 루트에 `QUALITY_CHECK.md` 생성 → 디렉토리별 분석 → 도구 실행 → 종합 리포트

**입력 예시:**
- "코드 품질 검사해줘" → Partial 모드 (변경사항 있으면), Full Audit (변경사항 없으면)
- "지난 10개 커밋 리뷰해줘" → Recent Commits 모드 (N=10)
- "전체 저장소 감사해줘" → Full Audit 모드

#### 기대 효과

- **회귀 조기 발견**: 커밋/PR 단계에서 잠재적 버그, 보안 취약점, API 계약 위반을 사전에 발견
- **테스트 커버리지 개선**: 구체적인 테스트 추가 지침을 제공하여 테스트 사각지대 제거
- **코드 유지보수성 향상**: 순환 의존성, 대형 함수, 불명확한 소유권 등 구조적 문제를 방향 제시
- **리뷰어 부담 감소**: 코드 리뷰 전에 quality-check를 먼저 실행하면 기본적인 문제가 정리된 상태에서 리뷰 가능
- **표준화된 품질 기준**: P1/P2/P3 등급 체계로 팀 내 일관된 품질 판단 기준 제공
- **도구 통합**: 기존 린트/테스트/타입체크를 자동 탐색하여 실행하고 결과를 리포트에 반영
- **코드 수정 제로**: 절대 코드를 수정하지 않으므로 품질 검사가 안전하게 실행됨

---

### startup-launch-workflow

#### 상세 설명

작은 SaaS 창업팀이 아이디어에서 런칭까지 가는 과정에서 반복적으로 막히는 지점을 **문제 정의 → 실행 → 검증 → 문서화 → GTM 메시지 → 재사용화** 루프로 바꿉니다.

단순히 "Codex로 이것저것 했다"를 나열하지 않고, 실제로 팀에게 일어난 병목을 중심으로 제품 정의, repo 기반 구현, 브라우저 QA, 보안 점검, feature docs, 랜딩 페이지/브로슈어/이메일 문구까지 하나의 실행 흐름으로 연결합니다.

#### 언제 사용하면 좋은가

| 상황 | 설명 |
|------|------|
| **창업 런칭 워크플로우 정리** | 아이디어 검증부터 런칭 준비까지 Codex 실행 흐름을 만들 때 |
| **SaaS 기능 end-to-end 실행** | 기능 정의, 구현, QA, 문서화, GTM 메시지를 한 번에 연결할 때 |
| **Codex 활용 사례 발표/문서화** | 우리 팀이 겪은 어려움, 돌파 과정, 성과를 스토리로 정리할 때 |
| **AGENTS.md/skills/scripts 체계화** | 반복 작업을 팀 SOP와 재사용 가능한 스킬로 남길 때 |
| **개발 결과의 마케팅 변환** | 구현된 기능을 랜딩 페이지, 브로슈어, CTA, 이메일 문구로 바꿀 때 |

#### 어떻게 사용하는가

1. **병목 식별** — 제품 정의, 협업 규칙, 런칭 전 검증, GTM 분리 중 현재 막힌 지점을 고릅니다.
2. **입력/출력 선언** — 고객 문제, repo, 문서, 기능 상태, 리스크와 기대 산출물을 정합니다.
3. **실행 루프 적용** — 제품 정의 → 구현/계획 → 검증 → 문서화 → 고객 메시지 변환 순서로 진행합니다.
4. **재사용화** — 반복되는 판단과 명령을 AGENTS.md, skill, checklist, prompt, script로 남깁니다.

#### 기대 효과

- **창업 병목 가시화**: 막연한 "런칭 준비"를 실제 병목과 실행 단위로 분해
- **Codex-specific 활용 강화**: repo, terminal, browser, git, AGENTS.md, skills, scripts를 하나의 흐름으로 연결
- **검증 포함**: 생성만 하는 것이 아니라 테스트, 브라우저 QA, 보안, 문서 반영까지 확인
- **GTM 연결**: 개발 결과를 고객 가치, 랜딩 페이지, 브로슈어, 이메일 메시지로 변환
- **재사용 가능한 팀 SOP**: 한 번 해결한 흐름을 다음 기능과 다음 팀원이 다시 쓸 수 있게 정리

---

### natural-marketing-copy

#### 상세 설명

한국어 랜딩 페이지, 브로슈어, CTA, 이메일, GTM 문구에서 **AI 티**, 번역투, 내부 추상어, 과장된 SaaS 표현을 제거하는 카피 정제 워크플로우입니다.

Monithub 홈페이지의 `copy:surface`, `copy:evaluate`, `copy:sanity`, `patina:gate` 흐름을 일반화한 실행 스크립트를 포함합니다. 문구를 감으로 다듬는 대신 고객 장면, 스토리 아크, 금지 표현, 문장 길이, claim 안정성을 반복 검증 가능한 gate로 점검합니다.

#### 언제 사용하면 좋은가

| 상황 | 설명 |
|------|------|
| **AI 티 제거** | 생성된 마케팅 문구가 번역투나 AI SaaS 문장처럼 읽힐 때 |
| **랜딩 페이지/브로슈어 카피** | hero, section, CTA, pricing/demo 문구를 고객 언어로 다듬을 때 |
| **GTM 메시지 변환** | 제품 기능 설명을 고객 가치, Before/After, 이메일 문구로 바꿀 때 |
| **copy QA gate 구축** | bundled scripts로 structured copy source, copy surface, sanity/eval/patina gate를 만들 때 |
| **claim 검증** | 자동 해결, 완전한 RCA, 무제한 같은 과장 claim을 막아야 할 때 |

#### 어떻게 사용하는가

1. **surface 식별** — hero, section, brochure, CTA, email, metadata 중 어디에 쓰이는 문구인지 먼저 구분합니다.
2. **copy surface 추출** — `scripts/copy-surface.mjs`로 JSON/JSONC/CMS/source copy에서 path + text 형태로 문구를 뽑습니다.
3. **차단 규칙 적용** — `scripts/copy-gate.mjs`로 내부 추상어, 번역투, 과장 표현, 시간 부사 충돌, 반복 설명 구조를 찾습니다.
4. **스토리 아크 확인** — 고객 장면, 현재 답답함, 다음 행동, 남는 결과, CTA가 이어지는지 봅니다.
5. **재검증** — `copy:evaluate`, `copy:sanity`, `patina:gate` 또는 수동 체크리스트로 다시 확인합니다.

#### 기대 효과

- **AI풍 문장 감소**: "제공합니다", "활용 가능합니다", "혁신적인" 같은 생성형 문구를 줄임
- **고객 언어 강화**: 내부 전략어 대신 고객이 겪는 장면과 행동으로 표현
- **과장 claim 방지**: 실제 제품 상태와 맞지 않는 자동화/완전 해결 표현을 차단
- **브로슈어 흐름 개선**: 기능 나열이 아니라 문제 → 탐색 → 결과 → 행동 흐름으로 정리
- **반복 가능한 카피 QA**: 감각 의존이 아니라 bundled scripts와 체크리스트로 재검증 가능

---

### monithub-issue-pr-workflow

#### 상세 설명

Monithub 레포지토리에서 기능 구현, 버그 수정, 문서화, 브랜치 생성, 커밋, PR 생성 전에 **기존 GitHub 이슈를 먼저 찾고**, 없으면 새 이슈를 만든 뒤 작업을 진행하도록 강제하는 워크플로우입니다.

큰 작업은 상위 이슈와 `[SubIssue]` 구조로 나누고, 구현 PR은 실제로 완료하는 서브 이슈에 `Closes #번호`, 상위 로드맵 이슈에는 `Related #번호`를 연결합니다. 또한 `docs/features/`, `docs/release/pre-deploy-discussions.md`, Swagger 문서 갱신 여부까지 함께 점검합니다.

#### 언제 사용하면 좋은가

| 상황 | 설명 |
|------|------|
| **Monithub 기능 구현 시작 전** | 기존 이슈 검색 또는 새 이슈 생성을 먼저 수행 |
| **큰 기능을 쪼개야 할 때** | parent issue + `[SubIssue]` 구조로 구현 단위를 나눔 |
| **문서화가 필요한 기능** | `docs/features/`와 pre-deploy 논의사항 갱신 여부 확인 |
| **PR 생성 전** | PR base branch, 관련 이슈, `Closes`/`Related` 연결 확인 |
| **API 변경 작업** | Swagger 문서와 예제 값 갱신 여부를 체크 |
| **더러운 작업 트리에서 작업할 때** | unrelated dirty files를 건드리지 않도록 먼저 확인 |

#### 어떻게 사용하는가

1. **자동 감지** — Monithub 레포에서 구현/문서/PR 작업을 시작하면 활성화됩니다.
2. **직접 호출** — `$monithub-issue-pr-workflow`를 명시해 사용할 수 있습니다.
3. **이슈 우선** — `gh issue list` 또는 GitHub connector로 기존 이슈를 검색합니다.
4. **없으면 생성** — 작업 전에 이슈를 만들고, 큰 작업은 서브 이슈로 나눕니다.
5. **PR 연결** — PR 본문에 `Closes #subIssue`, `Related #parentIssue`를 넣습니다.
6. **PR 메타데이터 설정** — assignee는 `@me`, 작업 유형별 label, repo별 project를 붙이고 reviewer는 요청하지 않습니다.

#### 기대 효과

- **작업 추적성 향상**: 모든 구현과 PR이 GitHub 이슈에 연결됨
- **기획 누락 방지**: 큰 기능을 구현 전에 parent/sub issue로 쪼개 범위를 명확히 함
- **문서 품질 유지**: 기능 문서, pre-deploy 논의, Swagger 갱신을 PR 전에 점검
- **리뷰 효율화**: PR이 어떤 이슈를 완료하는지 명확해져 리뷰 범위가 줄어듦
- **운영 리스크 감소**: 임시 정책과 production-readiness caveat를 문서에 남김

---

### monithub-feature-docs

#### 상세 설명

Monithub 기능 구현 과정에서 설계 문서, 의사결정 기록, 배포 전 논의사항을 코드 변경과 함께 유지하도록 돕는 문서화 워크플로우입니다.

기능별 문서는 `docs/features/`, 주요 기술 결정은 `docs/decisions/`, mock/fake/하드코딩/임시 정책처럼 운영 배포 전에 다시 확인해야 할 항목은 `docs/release/pre-deploy-discussions.md`에 기록합니다.

#### 언제 사용하면 좋은가

| 상황 | 설명 |
|------|------|
| **새 기능 시작** | 기능 배경, 목표, 범위, 구현해야 할 것, 미결정 사항을 먼저 문서화 |
| **기능 구현 중** | 현재 구현 상태, 다음 작업, 결정사항, 임시 구현을 계속 갱신 |
| **ADR 필요** | 저장소, API 방식, 배포 구조, 권한 모델 등 주요 기술 결정을 남길 때 |
| **배포 전 점검** | mock, fake response, hardcoded URL, 임시 계정/권한 정책을 추적할 때 |
| **PR 전 문서 확인** | 코드와 문서 상태가 일치하는지 확인할 때 |

#### 어떻게 사용하는가

1. **기능 문서 생성** — `docs/features/<feature-name>.md`를 만들고 기본 섹션을 채웁니다.
2. **상태 관리** — `미시작`, `진행 중`, `완료`, `보류`, `재검토 필요` 중 하나로 현재 상태를 유지합니다.
3. **임시값 추적** — mock, fake response, hardcoded URL, 임시 정책을 문서와 pre-deploy 논의사항에 남깁니다.
4. **ADR 분리** — 장기 구조나 보안/권한 모델 같은 큰 결정은 `docs/decisions/`에 따로 남깁니다.
5. **마무리 점검** — PR/커밋 전 문서 상태가 실제 구현과 맞는지 확인합니다.

#### 기대 효과

- **맥락 보존**: 왜 이 기능을 만들고 어떤 결정을 했는지 코드 밖에 남김
- **문서 누락 감소**: 구현과 문서 갱신이 분리되지 않도록 관리
- **배포 리스크 관리**: 임시 정책과 hardcoded 값이 운영에 남지 않도록 추적
- **리뷰 효율화**: 리뷰어가 기능 범위, 상태, 남은 리스크를 빠르게 파악
- **후속 작업 연결**: 다음에 바로 해야 할 것과 후속 구현 범위를 명확히 유지

## Installation

```bash
npx skills add Team-Discipline/skills
```

## Usage

설치 후 AI 에이전트가 관련 작업을 감지하면 자동으로 skill을 사용합니다.

직접 호출할 수도 있습니다:
- `커밋을 분할해줘` — git-commit-split 활성화
- `코드 품질을 검사해줘` — quality-check 활성화
- `$startup-launch-workflow` — 창업/런칭 병목을 Codex 실행 루프로 정리
- `$natural-marketing-copy` — 랜딩 페이지, 브로슈어, CTA 문구의 AI 티 제거
- `$monithub-issue-pr-workflow` — Monithub 이슈/PR 워크플로우 활성화
- `$monithub-feature-docs` — Monithub 기능 문서, ADR, pre-deploy 논의사항 갱신

## License

MIT
